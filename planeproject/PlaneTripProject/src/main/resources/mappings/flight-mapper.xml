<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="FlightDAO">
	<!-- 쿼리 작성 영역 -->
	<!-- 이름 추가 쿼리 -->
	<select id="viewFlight">
		SELECT * FROM FLIGHT 
		WHERE  AND  
	</select>
	
	<select id="viewAirport">
		SELECT * FROM AIRPORT 
		WHERE AND
	</select>
</mapper>